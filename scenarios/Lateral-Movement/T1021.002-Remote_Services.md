# T1021.002 REMOTE SERVICES: SMB/WINDOWS ADMIN SHARES (T1021.002) - PSEXEC LATERAL MOVEMENT CHAIN

This rule detects advanced lateral movement attacks that leverage Windows Administrative Shares (ADMIN$ or C$) combined with malicious service installations to execute remote commands. Attackers and frameworks like Impacket (psexec.py) heavily exploit administrative shares to drop an executable payload and immediately register it as a system service to gain an interactive SYSTEM shell on target hosts.

The analytics engine correlates two distinct log sources over a strict 2-minute time window on the same asset:

    Windows Security EventID 5145: Tracks an executable file (.exe) being written directly to an administrative network share (e.g., ShareName="\\*\ADMIN$").

    Windows System EventID 7045: Tracks the immediate installation of a new Windows service pointing to that exact dropped binary location (e.g., %systemroot%\<payload>.exe).

This correlation is highly effective because it maps the actual attack chain. Isolated admin share writes or service creations might occasionally happen during legitimate patch deployments, but when an executable is written to ADMIN$ and executed as a service within 120 seconds on the exact same host, it represents a definitive signature of a PsExec-style lateral movement sequence.


# A. Writing the Splunk Query

- [Splunk SPL](../../Rules/Splunk-SPL/Lateral-Movement/admin-shares.spl) 👈

# B. Testing the Rule

To simulate this advanced threat vector, an adversarial session is initiated from the attacker's Kali Linux machine using impacket-psexec. The attack exploits a compromised local administrator account, facilitated by a pre-configured registry bypass (LocalAccountTokenFilterPolicy set to 1), allowing full token elevation over network connections.

![Admin Shares](../../screenshots/admin-shares-2.png?v=2)

# C. Log Verification in Splunk 

The defensive logic triggers flawlessly when the core components of the kill chain intersect. As validated in admin-shares-1.png, the Splunk dashboard aggregates both Event 5145 and 7045 logs, delivering contextualized behavioral analysis:

    signals: Explicitly groups both admin_share_write and suspicious_service behaviors under a single timeline block.

    files & paths: Shows randomized binaries such as uFvzJAON.exe and lsuAS1qZ.exe dropping into %systemroot% (C:\Windows).

    svc: Extracts the distinct, randomly generated short-lived service names (tbrG and KETq).

    signal_types: The value 2 confirms that both required attack steps happened concurrently, providing the SOC analyst with actionable threat data and filtering out normal environmental noise.

![Splunk Search](../../screenshots/admin-shares-1.png?v=2)
